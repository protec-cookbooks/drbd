# This file is managed by Chef
# DO NOT EDIT THIS FILE BY HAND! - YOUR CHANGES WILL BE OVERWRITTEN

global {
	usage-count <%= node['drbd']['usage_count'] %>;
	# minor-count dialog-refresh disable-ip-verification
}

common {
	protocol <%= node['drbd']['common']['protocol'] %>;

	handlers {
		pri-on-incon-degr "<%= node['drbd']['handlers']['pri_on_incon_degr'] %>";
		pri-lost-after-sb "<%= node['drbd']['handlers']['pri_lost_after_sb'] %>";
		local-io-error "<%= node['drbd']['handlers']['local_io_error'] %>";
		# fence-peer "/usr/lib/drbd/crm-fence-peer.sh";
		# split-brain "/usr/lib/drbd/notify-split-brain.sh root";
		# out-of-sync "/usr/lib/drbd/notify-out-of-sync.sh root";
		# before-resync-target "/usr/lib/drbd/snapshot-resync-target-lvm.sh -p 15 -- -c 16k";
		# after-resync-target /usr/lib/drbd/unsnapshot-resync-target-lvm.sh;
	}

	startup {
		# wfc-timeout degr-wfc-timeout outdated-wfc-timeout wait-after-sb
	}

	disk {
		# on-io-error fencing use-bmbv no-disk-barrier no-disk-flushes
		# no-disk-drain no-md-flushes max-bio-bvecs
	}

	net {
		# sndbuf-size rcvbuf-size timeout connect-int ping-int ping-timeout max-buffers
		# max-epoch-size ko-count allow-two-primaries cram-hmac-alg shared-secret
		# after-sb-0pri after-sb-1pri after-sb-2pri data-integrity-alg no-tcp-cork
	}

	syncer {
		# rate after al-extents use-rle cpu-mask verify-alg csums-alg
	}
}
